import React from "react";
interface ScrollButtonProps {
    hidden: boolean;
    onClick: () => void;
    dir: "left" | "right";
}
declare function ScrollButton({ hidden, onClick, dir }: ScrollButtonProps): React.JSX.Element;
export default ScrollButton;
