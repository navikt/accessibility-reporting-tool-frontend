import React from "react";
import { FileItem } from "./Item.types";
interface ItemIconProps {
    isLoading?: boolean;
    file: FileItem;
    showError: boolean;
}
declare function ItemIcon({ isLoading, file, showError }: ItemIconProps): React.JSX.Element;
export default ItemIcon;
